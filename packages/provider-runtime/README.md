# @software-factory/provider-runtime

Runtime de providers do `software-factory 2.0`.

Este pacote concentra:

- registry de providers
- resolução de modelo
- health helpers
- fallback entre providers CLI
- adapters de execução

Objetivo: tirar da CLI a responsabilidade direta sobre providers e modelos.
