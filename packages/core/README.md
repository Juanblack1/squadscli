# @software-factory/core

Contratos puros do `software-factory 2.0`.

Este pacote concentra:

- tipos compartilhados
- contratos de workflow
- contratos de provider
- estado de execução
- referências de artefatos

Objetivo: servir de base estável para `cli`, `server`, `web` e `mcp`.
